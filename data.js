const { lemonyFresh, mods, users, knownTags, lemCmds, wordBank,
    commonNicknames, startingLemons, hangmanWins // unclaimed legacy memory tables
} = require(`./memory.json`)

module.exports = {
    lemonyFresh,
    mods,
    users,
    knownTags,
    lemCmds,
    wordBank,
    commonNicknames,
    startingLemons,
    hangmanWins
}
