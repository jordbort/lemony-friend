// Global settings
const funNumberCount = 25
const funNumberTotal = 50
const options = {
    listening: true,
    sayOnlineMsg: true,
    FIRST_CONNECTION: true,
    debug: true
}

module.exports = {
    funNumberCount,
    funNumberTotal,
    options
}
